export function SectionTitle({ children }: { children: React.ReactNode }) {
    return <h3 className="text-sidebar-foreground/40 mb-4 text-[11px] font-bold tracking-[0.15em] uppercase">{children}</h3>;
}
